name: Semgrep

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "27 4 * * 1"

permissions:
  contents: read

jobs:
  semgrep:
    name: SAST scan
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6

      - name: Run Semgrep CI
        run: semgrep ci --config p/javascript --config p/typescript --config p/security-audit --config p/owasp-top-ten
